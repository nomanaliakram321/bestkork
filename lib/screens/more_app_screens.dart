import 'package:flutter/material.dart';
class MoreAppScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Comming Soon',style: TextStyle(color: Colors.green[500]),),
      ),
    );
  }
}
